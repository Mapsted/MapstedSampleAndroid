buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.4.32"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven {
            url "http://mobilesdk.mapsted.com:8081/artifactory/gradle-mapsted"
            credentials {
                username = "sdksample"
                password = "Sample20!9"
            }
        }

    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
